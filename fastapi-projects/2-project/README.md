# Introduction

In this project, we have level up by introducing pydantic.

```bash
# 1. create .venv
uv venv .venv

# 2. activate .venv
source .venv/bin/activate

# 3. install dependencies
uv sync

# 4. run app
uv run uvicorn src.main:app --reload
```